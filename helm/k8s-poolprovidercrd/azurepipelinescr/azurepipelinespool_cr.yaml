apiVersion: dev.azure.com/v1alpha1
kind: AzurePipelinesPool
metadata:
  name: azurepipelinespool-operator
  namespace: azuredevops
spec:
  controllerImage: prebansa/k8s-poolprovider:v2.16
  buildkitReplicas: 1
  agentPools:
  - name: windows
    spec:
      containers:
      - name: vsts-agent
        image: prebansa/myagent:v5.17
        env:
        - name: RUNNING_ON
          valueFrom:
            configMapKeyRef:
              name: kubernetes-config
              key: type
  - name: linux
    spec:
      containers:
      - name: vsts-agent
        image: prebansa/myagent:v6.0
        env:
        - name: RUNNING_ON
          valueFrom:
            configMapKeyRef:
              name: kubernetes-config
              key: type
