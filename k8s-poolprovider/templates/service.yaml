apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.name }}
  labels:
    app: {{ .Values.app.name }}
spec:
  type: NodePort
  ports:
    - port: 8082
  selector:
    app: {{ .Values.app.name }}
    tier: frontend
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    app: {{ .Values.app.name }}
  name: {{ .Values.app.name }}
spec:
  backend:
    serviceName: {{ .Values.app.name }}
    servicePort: 8082